{{{
  exports({ to: app.makePath('app/jobs/' + filename + '.ts') })
}}}

import { Breeze, Job } from '@likable-hair/breeze';

type {{ className }}Payload = {};

type {{ className }}Result = {};

export default class {{ className }} extends Breeze<{{ className }}Payload, {{ className }}Result> {
  public key = '{{ className }}' as const;

  async handle(job: Job<{{ className }}Payload, {{ className }}Result, string>): Promise<{{ className }}Result> {
    const payload = job.data; 

    console.info('{{ className }} job handled', payload);

    return {}; // Return an empty object since {{ className }}Result is empty
  }
}
